{
  "rules": {
    "ui:field": "Table",
    "items": {
      "rulesetId": {
        "ui:widget": "ApiLookup",
        "ui:options": {
          "lookupProvider": "Rulesets",
          "columnWidth": "42%"
        }
          
      },
      "replaceExpr": {
        "ui:widget": "JsInputWithoutPopup",
        "ui:placeholder": "Enter replace expression",
        "ui:default": "'REDACTED'",
        "ui:options": {
          "columnWidth": "42%"
        }
      },
      "disabled": {
        "ui:title": "Enabled",
        "ui:options": {
          "columnWidth": "16%",
          "inverted": true
        }
      }
    }
  },
  "fields": {
    "ui:field": "Tags",
    "ui:placeholder": "Field names"
  },
  "excludeFields": {
    "ui:field": "Tags",
    "ui:placeholder": "Start typing to add exclusion fields"
  },
  "flags": {
    "ui:field": "Table",
    "items": {
      "name": {
        "ui:placeholder": "Enter field name"
      },
      "value": {
        "ui:widget": "JsInputWithoutPopup",
        "ui:placeholder": "Enter value expression"
      }
    }
  },
  "includeDetectedRules": {
    "ui:title": "Add matched rulesets",
    "ui:default": false
  },
  "ui:options": {
      "groups": {
        "advanced": {
            "title": "Advanced Settings",
            "collapsed": true,
            "properties": ["flags", "includeDetectedRules"]
        }
      }
    }
}
